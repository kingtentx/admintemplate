
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>图文列表组件</title>
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <link rel="stylesheet" href="~/layuiadmin/layui/css/layui.css" media="all">
    <link rel="stylesheet" href="~/layuiadmin/style/admin.css" media="all">
</head>
<body>
    <div class="layui-fluid">
        <div class="layui-row layui-col-space15">
            <div class="layui-col-md12">
                <div class="layui-card">                   
                    <div class="layui-card-body">
                        @*<div class="conf-view">
                            <div style="padding-bottom: 10px;">

                                <a class="layui-btn layuiadmin-btn-admin" data-type="add" href="/Page/BannerEdit">添加</a>

                            </div>
                        </div>*@
                        <table class="layui-hide" id="app-table" lay-filter="app-table"></table>

                        @*<script type="text/html" id="app-table-bar">

                            <a class="layui-btn layui-btn-normal layui-btn-xs" lay-event="edit"><i class="layui-icon layui-icon-edit"></i>编辑</a>

                            <a class="layui-btn layui-btn-danger layui-btn-xs" lay-event="del"><i class="layui-icon layui-icon-delete"></i>删除</a>

                        </script>*@

                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="~/layuiadmin/layui/layui.js"></script>
    <script src="~/js/site.js"></script>
    <script>
        layui.config({
            base: '../../layuiadmin/' //静态资源所在路径
        }).extend({
            index: 'lib/index' //主入口模块
        }).use(['index', 'table'], function () {
            var $ = layui.$,
                form = layui.form,
                table = layui.table;

            table.render({
                elem: '#app-table'
                , url: '/Page/GetTagsList?type=' + getQueryString('type')
                , response: {
                    statusName: 'code' //规定数据状态的字段名称，默认：code
                    , statusCode: '1000 ' //规定成功的状态码，默认：0
                    , msgName: 'msg' //规定状态信息的字段名称，默认：msg
                    , countName: 'count' //规定数据总数的字段名称，默认：count
                    , dataName: 'data' //规定数据列表的字段名称，默认：data
                }
                , cols: [[
                    { type: 'checkbox' }
                    , { field: 'tagsId', width: 80, title: 'ID' }
                    , { field: 'tagsName', width: 200, title: '名称' }
                   // , { align: 'center', fixed: 'right', toolbar: '#app-table-bar', hide: isShow() }
                ]]
                , request: {
                    pageName: 'pageIndex' //页码的参数名称，默认：page
                    , limitName: 'pageSize' //每页数据量的参数名，默认：limit
                }
                , page: true
            });

            //监听表格复选框选择
            table.on('checkbox(app-table)', function (obj) {
                var type = getQueryString('type');
                var checkStatus = layui.table.checkStatus('app-table').data;
                var new_obj = [];
                for (var i = 0; i < checkStatus.length; i++) {
                    var data = {
                        'id': checkStatus[i].tagsId,
                        'name': checkStatus[i].tagsName
                    };
                    new_obj.push(data);
                }
                localStorage.setItem('tagslist_data_' + type, JSON.stringify(new_obj));
            });

            ////监听工具条
            //table.on('tool(app-table)', function (obj) {
            //    var data = obj.data;
            //    if (obj.event === 'del') {
            //        layer.confirm("确认删除?", function (index) {
            //            var loading = layer.load(0, {
            //                shade: 0.3
            //            });
            //            $.ajax({
            //                url: "/Page/BannerDelete",
            //                data: { 'id': data.id },
            //                type: "post",
            //                dataType: "json",
            //                success: function (res) {
            //                    console.log(res);
            //                    layer.close(loading);
            //                    if (res.code == 1000) {
            //                        layer.msg(res.msg);
            //                        obj.del();
            //                        layer.close(index);
            //                        //location.reload('#app-table'); //删除成功后再刷新
            //                    }
            //                    else {
            //                        layer.msg(res.msg);
            //                    }
            //                }
            //            });
            //        });
            //    } else if (obj.event === 'edit') {
            //        console.log(data);
            //        window.location.href = '/Page/BannerEdit/' + data.id;
            //    }
            //});

            ////监听搜索
            //form.on('submit(app-search)', function (data) {
            //    var field = data.field;

            //    //执行重载
            //    table.reload('app-table', {
            //        where: field
            //    });
            //});

            //function isShow() {
            //    var view_type = getQueryString('flag');
            //    if (view_type != null) {
            //        $('.conf-view').hide();
            //        return true;
            //    }
            //    return false;
            //}
        });
    </script>
</body>
</html>